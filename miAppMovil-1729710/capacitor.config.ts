import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'miAppMovil-1729710',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
